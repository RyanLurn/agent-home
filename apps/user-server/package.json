{
  "name": "@repo/user-server",
  "type": "module",
  "private": "true",
  "scripts": {
    "start": "bun run src/serve.ts",
    "try": "bun run src/try.ts",
    "build": "tsdown",
    "check-types": "tsc --noEmit",
    "lint": "eslint --cache",
    "test": "bun run check-types && bun test"
  },
  "dependencies": {
    "@repo/core": "workspace:*",
    "@repo/db": "workspace:*",
    "@t3-oss/env-core": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/tsdown-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}
